# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /home/jared/datasets # dataset root dir
train: images/training  # train images (relative to 'path') 4 images
val: images/val # val images (relative to 'path') 4 images
test:  # test images (optional)

# Keypoints
kpt_shape: [8, 3]  # number of keypoints, number of dims (2 for x,y or 3 for x,y,visible)
flip_idx: [1, 0, 3, 2, 5, 4, 7, 6]

# Classes dictionary
names:
  0: red
  1: blue
  2: green